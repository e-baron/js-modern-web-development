import { GenericFunctionalComponent } from "../utils/render.js";

const PrintError = (props) => {
  if (props.isUpdated) return;
  // deals with the new component specific props, if any...  
};



export default GenericFunctionalComponent(PrintError);
